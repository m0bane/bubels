CREATE DATABASE users;
CREATE TABLE uzytkownicy (
    id INT AUTO_INCREMENT PRIMARY KEY,
    login VARCHAR(255) NOT NULL UNIQUE,
    haslo VARCHAR(20) NOT NULL CHECK (CHAR_LENGTH(haslo) BETWEEN 10 AND 20)
);